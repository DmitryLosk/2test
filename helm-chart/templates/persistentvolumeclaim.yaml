apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tutorial-app-pvc
spec:
  accessModes:
    - {{ .Values.persistence.accessModes | quote }}
  resources:
    requests:
      storage: {{ .Values.persistence.size }}